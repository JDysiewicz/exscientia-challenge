import styled from "@emotion/styled";

// styled components used in more than one component

export const SectionTitle = styled.h2`
	font-family: var(--font-title);
	font-size: var(--font-size-subtitle);
`;
